const PROFILE_STATUS = {
  PERSONAL_DETAILS: {
    name: "פרטים אישיים",
    isFilled: true,
  },
  PREFERENCES: {
    name: "שאלון אישי",
    isFilled: true,
  },
  FIRST_ORDER_DETAILS: {
    name: "נתוני צו ראשון",
    isFilled: false,
  },
};

window.onload = async () => {};

const createProfileStatusItem = () => {};

const fetchProfileStatus = (id) => {};

const fetchProfileImage = (id) => {};
